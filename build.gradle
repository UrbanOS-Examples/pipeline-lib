apply plugin: 'groovy'

repositories {
    mavenCentral()
}

dependencies {
    compile group: 'org.codehaus.groovy', name: 'groovy-all', version: '2.4.5'
}


sourceSets {
  main {
    groovy {
      srcDirs = ['src/org/scos/pipeline', 'vars']
    }
  }
}